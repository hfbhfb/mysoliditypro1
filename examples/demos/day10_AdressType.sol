pragma solidity ^0.4.25;

contract TestAddress {
    // 20 bytes 组成
    // Wei 是 ethereum 最小单位
    // <address>.send(uint256 amount)
    // 2300 gas 消耗
    // Transfer vs Send
    function() public payable {}
}
