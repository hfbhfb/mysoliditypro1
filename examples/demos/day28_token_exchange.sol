pragma solidity ^0.4.25;


import "day20_ERC20.sol";

contract TokenExchange {
    // From => UserA,TokenA
    address fromAddress;
    address fromToken;
    uint256 fromAmount;
    // To 
    address toAddress;
    address toToken;
    uint256 toAmount;

    // 假设 UserA 已经 Approve
    function CreateExchange{

    }
}
